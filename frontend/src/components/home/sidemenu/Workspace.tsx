import styled from '@emotion/styled';
import React from 'react';
import MenuTemplate from './MenuTemplate';


const Workspace = () => {
    return (
        <MenuTemplate title='워크스페이스' />
    );
};

export default Workspace;