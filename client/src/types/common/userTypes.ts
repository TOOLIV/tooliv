export type userBadgeTypes = {
  name: string;
  email: string;
  onClick: () => void;
};

export type userItemTypes = {
  name: string;
  email: string;
  userCode: string;
  onClick: () => void;
};

export type userListTypes = {
  name: string;
  email: string;
  userCode: string;
};
