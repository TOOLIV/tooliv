import "@emotion/react";

declare module "@emotion/react" {
  interface Theme {
    bgColor: string;
    sideBgColor: string;
    pointColor: string;
    secondPointColor: string;
    lightPointColor: string;
    textColor: string;
  }
}
